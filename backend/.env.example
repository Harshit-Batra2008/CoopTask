# Copy this file to .env and fill in real values when a database is set up in Phase 1.
# No database is required yet for Phase 0 — the server runs without this file.

PORT=4000

# Example only — not used until Phase 1 (schema/migrations) begins
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/trustserve?schema=public"
