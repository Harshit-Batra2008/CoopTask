# Copy to .env if your backend runs somewhere other than the default.
# Not required for local development — src/utils/api.js falls back to
# http://localhost:4000 automatically.

VITE_API_URL=http://localhost:4000
